# Screenshots

Drop screenshots/GIFs here to populate the images referenced in the main README:

- `review.png` — the daily review queue with the Again/Hard/Good/Easy buttons
- `mastery.png` — the pattern mastery skill map
- `dashboard.png` — the Recharts dashboard
- `complete.png` — the "Daily reviews complete!" confetti state
- `loopcode-demo.gif` — a short screen recording of a review session

Capture them after running `npm run dev` and opening http://localhost:5173.
